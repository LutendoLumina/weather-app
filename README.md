# 🌦️ WeatherDetector (Django)

WeatherDetector is a simple Django web application that allows a user to enter a city name and view current weather information using the OpenWeather API.

---

## 📌 What the App Does

- User enters a city name
- The app displays:
  - Country code
  - Coordinates
  - Temperature
  - Pressure
  - Humidity
- Weather data is fetched from the OpenWeather API
- API keys are securely stored using environment variables

---

## 🛠️ Built With

- Python
- Django
- HTML / CSS
- OpenWeather API

